{
  "name": "ip-address-tracker-master",
  "version": "1.0.0",
  "description": "IP address tracker",
  "private": true,
  "homepage": "https://maatzie.github.io/IP_tracker",
  "main": "index.js",
  "scripts": {
    "start": "parcel index.html --open",
    "build": "parcel build index.html --out-dir=build --public-url ./",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "gh-pages": "^4.0.0",
    "parcel-bundler": "^1.12.5"
  },
  "dependencies": {
    "leaflet": "^1.9.2"
  }
}
